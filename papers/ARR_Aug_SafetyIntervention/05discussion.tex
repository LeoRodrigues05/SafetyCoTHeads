% =========================================================================
% 5. DISCUSSION   [~0.5 page]
% GOAL: zoom out from "our grid" to "why the instrument matters." No new results.
% =========================================================================
\section{Discussion}
\label{sec:discussion}

% ---- ¶1 (What the metric buys future work): decomposing "removes safety" into P,Q,S
%      and baseline-correcting each gives a comparable, reusable number that survives
%      porting across models whose base safety differs. The (P,Q,S) vector + Pareto
%      front keeps the decision transparent; the scalar answers the one-number reviewer.

% ---- ¶2 (Mechanism as the "why" behind S): the 12-label pathway taxonomy
%      (recognition / refusal-dynamics / rationalisation / execution) is reported as a
%      descriptive dominant_pathway histogram, explaining HOW safety reasoning changes
%      on a cell without contaminating a comparable scalar. One worked example.

% ---- ¶2b (What the grid says about the localization claims): read the findings back
%      through the entailments frame (§3.1). At matched strength, the direction-based
%      hypotheses pass the signature (necessity + selectivity + depth); head/neuron
%      localizations mostly fail necessity, and the base_own control shows discovery
%      returns confident candidates even where there is no mechanism to find.
%      Consistent with safety being low-rank and distributed rather than a small
%      circuit. State strictly as evidence about the CLAIMS at these budgets --- not
%      "safety is a direction".

% ---- ¶2c (Epistemic humility --- necessary, not sufficient): the behavioural
%      signature is necessary evidence of mechanism identification, never sufficient.
%      High P shows the target is causally sufficient to DISRUPT safety, not that it
%      IS the mechanism --- downstream or correlated substrates can score identically,
%      and interventions with identical signatures form an equivalence class the
%      metric cannot break. The metric grades localization claims on their own
%      operational terms; certifying ontology needs circuit-level causal analysis
%      (complementary, not replaced). This pre-empts the mech-interp objection.

% ---- ¶3 (From suppression to defence): the metric is orientation-agnostic --- a
%      defence eval flips the sign of P and reuses Q, S unchanged, so the same
%      instrument scores both breaking and hardening a model.

% ---- ¶4 (Scope / what generalises): 7--8B here; the axes and baseline-correction are
%      scale-free by construction; generalising beyond suppressive interventions and
%      beyond 8B is future work (forward-ref Limitations).

\TODO{Discussion is outline-only. Write ¶1--¶4.}
